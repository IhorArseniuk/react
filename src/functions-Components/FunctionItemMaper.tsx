export const FunctionItemMaper = (item) => {
    return (
        Object.entries(item).map(([key, value],index) => {
            if(typeof value === 'object'){

            }
        })
    )

    }

    ;
};
