import type {SimsonType} from "../../models/simsons.ts";
import type {FC} from "react";

export type CharacterType={
    simson:SimsonType
}
export const CharacterComponent:FC<CharacterType> = (simson) => {
    return (

    );
};
